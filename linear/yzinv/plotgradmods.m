function h = plotgradmods(DEPTHS,MODELS,lintype)%PLOTGRADMODS Plot a gradient model (or models)%%PLOTGRADMODS(DEPTHS,MODELS) plots the gradient model arrays in MODELS versus% the DEPTHS given (DEPTHS should be positive downwards).  At least one of the% dimensions of the model array must be equal to length(DEPTHS).  If both% are, then PLOTGRADMODS assumes that each column is a different model.% Each depth corresponds to the value at that depth.  If there is a% discontinuity, two rows will have the same depth but different other% values.%PLOTGRADMODS(DEPTHS,MODELS,'linetype') plots the model(s) with the given% linetype or symbol.if (nargin<2)  error('plotgradmods() needs at least two arguments')end[M,N] = size(MODELS);L = length(DEPTHS);if (M==L) % the default way, we're happy		L = M; %i.e., do nothingelseif (N==L)  MODELS = MODELS';  [M,N] = size(MODELS);else  error('No dimension of model array equal to dim of depth array');endif (nargin<3)  h = plot(MODELS,-DEPTHS+DEPTHS(1));else  h = plot(MODELS,-DEPTHS+DEPTHS(1),lintype);end